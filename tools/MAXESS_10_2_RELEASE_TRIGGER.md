# MAXESS 10.2 release trigger

The 10.2 hero-first visual pass is now part of the authoritative Results build pipeline.

This marker exists only to trigger a fresh Actions run after the workflow was updated. The actual presentation implementation is `tools/maxess_results_visual_pass_10_2.py`.
