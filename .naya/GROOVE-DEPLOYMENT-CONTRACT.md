# MAXESS — GROOVE DEPLOYMENT CONTRACT

Status: AUTHORITATIVE DEPLOYMENT CONTROL DOCUMENT
Updated: 2026-08-16

## PURPOSE

Eliminate the final ambiguity between GitHub engineering output and the human-facing Results page.

GitHub can prove that source bytes changed. It cannot, by itself, prove that the external Groove page consumed those bytes.

This contract defines the exact release chain.

## RELEASE OBJECT

Product: MAXESS Results

Authoritative engineering source:

`MAXESS-RESULTS-10-GROOVE.html`

Repository:

`SoulSchoolAcademy/maxess`

Branch:

`main`

Public verification target:

`https://results.nayanet.xyz/`

## REQUIRED EXTERNAL DEPLOYMENT SOURCE

The external publisher for `results.nayanet.xyz` must be identified before live parity can be proven.

Required facts:

1. Which Groove page/funnel/site owns `results.nayanet.xyz`?
2. Which Groove page element contains the Results code?
3. Is the current public code pasted directly into Groove, loaded from a URL, or generated by another artifact?
4. If loaded from a URL, which exact URL?
5. If pasted directly, which exact page/section is the authoritative editor location?
6. What publication action makes the edited Groove page live?
7. Is there a cache/CDN layer that can delay public verification?

Until these facts are known, the deployment state is:

**BLOCKED — EXTERNAL SOURCE UNKNOWN**

## REQUIRED RELEASE PACKAGE

The deployment package must contain the exact authoritative Results experience, including:

- Results renderer
- Result Contract boundary
- deterministic development fixture behavior, if still needed
- Naya integration
- NayaNET Page Code foundation
- responsive behavior
- accessibility behavior
- reduced-motion behavior
- print behavior
- CTA destination wiring
- required manifest markers

Do not assemble a reduced preview for Groove when the authoritative experience requires the full page.

## RELEASE PROOF

A release is valid only when all gates pass:

```text
SOURCE BASELINE
    ↓
IMPLEMENTATION
    ↓
SOURCE DIFF
    ↓
DETERMINISTIC QA
    ↓
GROOVE ARTIFACT
    ↓
GROOVE PUBLISH
    ↓
PUBLIC FETCH
    ↓
MARKER/PARITY CHECK
    ↓
VISUAL OSCAR REVIEW
    ↓
LIVE VERIFIED
```

## ZERO-CHANGE GATE

If the authoritative source or deployment artifact does not materially change when a change was requested:

**BLOCKED — ZERO-CHANGE EXECUTION**

## PARITY GATE

The public page must expose the current authoritative Results markers and the intended experience structure.

At minimum, the live page must prove the presence of:

- `MAXESS-RESULTS-CONTRACT-1`
- `MAXESS-RESULTS-EXPERIENCE-MANIFEST-1`
- current Naya/Results release markers
- current NayaNET foundation markers

If those are missing:

**BLOCKED — DEPLOYMENT PARITY FAILURE**

## HUMAN VERIFICATION

Code parity is necessary but not sufficient.

After the public page is updated, review the actual human experience at desktop and mobile widths for:

- full-width composition;
- hero hierarchy;
- score clarity;
- five-dimension visualization;
- 18 AI pathways;
- Naya transition;
- NayaNET foundation;
- button/icon quality;
- CTA hierarchy;
- no horizontal overflow;
- accessibility;
- reduced motion;
- performance;
- visual continuity.

## CURRENT BLOCKER

The repository is now structured so that another AI can identify the authoritative source and enforce proof. The remaining blocker is external: the actual Groove editing/publishing source for `results.nayanet.xyz` is not exposed through the connected GitHub environment.

No AI should claim the public Results page is live-verified until that external source is identified and published.
