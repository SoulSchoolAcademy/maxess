# MAXESS — GROOVE DEPLOYMENT CONTRACT

Status: AUTHORITATIVE DEPLOYMENT CONTROL DOCUMENT
Version: 1.1
Updated: 2026-08-16

## PURPOSE

Eliminate the ambiguity between GitHub engineering output and the human-facing Results page.

GitHub can prove that source bytes changed. It cannot, by itself, prove that the external Groove page consumed those bytes.

This contract defines the exact release chain and the smallest possible external deployment test.

## RELEASE OBJECT

Product: MAXESS Results

Authoritative engineering source:

`MAXESS-RESULTS-10-GROOVE.html`

Repository:

`SoulSchoolAcademy/maxess`

Branch:

`main`

Public verification target:

`https://results.nayanet.xyz/`

## CURRENT STATUS

GitHub source-of-truth governance is active on `main`.

The public target is not in parity with the authoritative engineering artifact.

The connected environment has no authenticated Groove editor/publishing integration. Therefore the external publish stage cannot be honestly marked complete from GitHub alone.

Status:

**BLOCKED — EXTERNAL GROOVE PUBLISH ACTION NOT CONNECTED**

This is now a hard boundary, not an invitation to guess.

## REQUIRED EXTERNAL DEPLOYMENT SOURCE

Before live parity can be proven, identify:

1. Which Groove site/funnel owns `results.nayanet.xyz`.
2. Which exact Groove page owns the route.
3. Which exact page element contains the Results code.
4. Whether the code is pasted directly, loaded from a URL, or generated by another artifact.
5. If loaded from a URL, the exact URL.
6. If pasted directly, the exact page/section editor location.
7. The exact publish action that makes the change live.
8. Any cache/CDN behavior that can delay verification.

## MINIMAL DEPLOYMENT PROBE

Do NOT begin by publishing the complete MAXESS page.

First prove the delivery channel with the smallest possible harmless payload:

```html
<div id="maxess-deployment-probe" data-maxess-deployment-probe="2026-08-16">
  MAXESS DEPLOYMENT PROBE — NAYA LAW
</div>
```

Expected proof chain:

```text
Probe source
   ↓
Groove editor
   ↓
Publish
   ↓
https://results.nayanet.xyz/
   ↓
Public fetch
   ↓
`maxess-deployment-probe` found
```

If the probe does not appear, STOP. Do not touch the MAXESS implementation. The failure is in the external delivery path.

If the probe appears, immediately replace the probe with the complete authoritative Results deployment payload and repeat the same live verification.

## REQUIRED RELEASE PACKAGE

The deployment package must contain the exact authoritative Results experience, including:

- Results renderer
- Result Contract boundary
- deterministic development fixture behavior, if still needed
- Naya integration
- NayaNET Page Code foundation
- responsive behavior
- accessibility behavior
- reduced-motion behavior
- print behavior
- CTA destination wiring
- required manifest markers

Do not assemble a reduced visual prototype when the authoritative experience requires the full page.

## RELEASE PROOF

A release is valid only when all gates pass:

```text
SOURCE BASELINE
    ↓
IMPLEMENTATION
    ↓
SOURCE DIFF
    ↓
DETERMINISTIC QA
    ↓
EXACT DEPLOYMENT PAYLOAD
    ↓
MINIMAL DEPLOYMENT PROBE
    ↓
GROOVE PUBLISH
    ↓
PUBLIC FETCH
    ↓
MARKER/PARITY CHECK
    ↓
VISUAL OSCAR REVIEW
    ↓
LIVE VERIFIED
```

## ZERO-CHANGE GATE

If the authoritative source or required deployment payload does not materially change when a change was requested:

**BLOCKED — ZERO-CHANGE EXECUTION**

## PARITY GATE

The public page must expose the current authoritative Results markers and intended experience structure.

At minimum, the live page must prove the presence of:

- `MAXESS-RESULTS-CONTRACT-1`
- `MAXESS-RESULTS-EXPERIENCE-MANIFEST-1`
- current Naya/Results release markers
- current NayaNET foundation markers

If those are missing:

**BLOCKED — DEPLOYMENT PARITY FAILURE**

## HUMAN VERIFICATION

Code parity is necessary but not sufficient.

After public deployment, review the actual human experience at desktop and mobile widths for:

- full-width composition;
- hero hierarchy;
- score clarity;
- five-dimension visualization;
- 18 AI pathways;
- Naya transition;
- NayaNET foundation;
- button/icon quality;
- CTA hierarchy;
- no horizontal overflow;
- accessibility;
- reduced motion;
- performance;
- visual continuity.

## HARD RULE

No AI may claim the public Results page is live-verified merely because:

- GitHub changed;
- a commit succeeded;
- a workflow passed;
- an HTML file exists;
- a preview renders;
- a deployment was attempted.

Live verification requires the actual public target to expose the new release markers and the requested user-facing result.
