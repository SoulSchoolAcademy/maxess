name: Legacy Final MAXESS Results 10 Release (disabled)
on:
  workflow_dispatch:
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Disabled. The authoritative Results publisher is build-final-results-groove.yml.'
